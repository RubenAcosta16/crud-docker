# Dependencias de Node.js (Nunca se suben, pesan mucho y se reconstruyen en Docker)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de entorno local / Secretos (Contraseñas, tokens de API)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Carpetas de compilación o datos locales de Docker (si aplicara)
.docker/